# Optional. Leave unset locally — Vite proxies /api to the server.
# In production (Vercel), set to your deployed API origin (no trailing slash):
# VITE_API_ORIGIN=https://your-api.onrender.com

# VITE_API_ORIGIN=
