# Production (Vercel): required — your Express API origin, no trailing slash.
# Example: VITE_API_ORIGIN=https://codearena-x-api.onrender.com

# Optional: set to "true" on Vercel to log auth/API diagnostics in the browser console.
# VITE_DEBUG_AUTH=true
